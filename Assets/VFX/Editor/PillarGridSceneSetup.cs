using UnityEngine;

/// <summary>
/// Legacy placeholder kept so the assembly still compiles after removing menu-driven scene creation.
/// Scene creation/setup is now done directly in the current scene via MCP/editor actions.
/// </summary>
public static class PillarGridSceneSetup
{
}
